{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "integratedTerminal"
            // Build is handled by CMake Tools extension.
            // Use Ctrl+Shift+B or status bar Build button before F5,
            // or use CMake Tools' own debug launch (Ctrl+F5).
        }
    ]
}
